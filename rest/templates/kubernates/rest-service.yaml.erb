apiVersion: v1
kind: Service
metadata:
  name: rest-service-<%= @graph_id %>
  namespace: <%= @organizationID %>
  labels:
    app: rest-app-<%= @graph_id %>
spec:
  selector:
    app: rest-app-<%= @graph_id %>
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

