apiVersion: v1
kind: Service
metadata:
  name: rest-service-<%= @ID %>
  namespace: <%= @organizationID %>
  labels:
    app: rest-app-<%= @ID %>
spec:
  selector:
    app: rest-app-<%= @ID %>
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

